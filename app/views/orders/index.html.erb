<% if notice %>
<p id ="notice"><%= notice %></p>
<% end %>

<h1>Listing Orders</h1>


<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Ime</th>
      <th>Adresa</th>
      <th>Email</th>
      <th>Nacin placanja</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.name %></td>
        <td><%= order.address %></td>
        <td><%= order.email %></td>
        <td><%= order.pay_type %></td>
        <td><%= link_to 'Prikazi', order, :class=>"btn btn-default" %></td> 
        <td><%= link_to 'Izbrisi', order, method: :delete, data: { confirm: 'Are you sure?' }, :class=>"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>



<%= link_to 'New Order', new_order_path %>
<p><%= will_paginate @orders, :previous_label => 'Prethodna', :next_label => 'Sledeća' %></p>
